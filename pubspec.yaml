name: myteamapp
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_lints: ^3.0.1
  provider: ^6.1.1
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  shared_preferences: ^2.2.2
  image_picker: ^1.0.4
  url_launcher: ^6.2.1
  file_picker: ^6.1.1
  animated_theme_switcher: ^2.0.9
  flutter_countdown_timer: ^4.1.0
  advance_pdf_viewer: ^2.0.1
  flutter_pdfview: ^1.3.2
  cached_network_image: ^3.3.0  # Add this line for cached_network_image

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.7

flutter:
  assets:
    - assets/hsnc_logo.png
    - assets/instagram_icon.png
  uses-material-design: true
