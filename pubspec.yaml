name: myteamapp
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_lints: ^3.0.1  # Add this line for flutter_lints
  provider: ^6.0.6
  shared_preferences: ^2.0.7
  image_picker: ^0.8.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.6

flutter:
  assets:
    - assets/hsnc_logo.png
  uses-material-design: true
